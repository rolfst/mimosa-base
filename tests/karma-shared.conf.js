
module.exports = function(config) {
    config.set({
        basePath: '../',

        // Start these browsers, currently available:
        // - Chrome
        // - ChromeCanary
        // - Firefox
        // - Opera
        // - Safari (only Mac)
        // - PhantomJS
        // - IE (only Windows)
        // - iOS
        browsers: ['PhantomJS'],

    });
};
